{
  "name": "mobile-simulator-chrome-extension",
  "version": "0.1.0",
  "description": "A Chrome extension that previews any website inside an accurately sized mobile or tablet viewport.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "zip": "npm run build && cd dist && zip -r ../mobile-simulator-extension.zip ."
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheDukeElephant/mobile-simulator-chrome-extension.git"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.25",
    "@types/chrome": "^0.0.270",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
